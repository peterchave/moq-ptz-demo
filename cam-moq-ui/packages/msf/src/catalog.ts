/**
 * Backward-compatible re-exports from catalog-msf00.
 *
 * @see catalog-msf00.ts for the actual implementation.
 * @module
 */

export { parseMsfCatalog as parseCatalog, parseMsfCatalog, isDelta } from './catalog-msf00.js';
